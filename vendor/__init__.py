"""Third-party readers carried here verbatim, so Arc2Lite installs nothing.

Both files are pure Python standard library and are copied unchanged from
their own repositories. Fix a defect upstream and re-vendor rather than
editing a copy here; vendored.json records the commit each copy came from and
the sha256 of the file, so a copy that has drifted is one command away from
being caught.
"""
